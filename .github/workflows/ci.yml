name: CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      date: ${{ steps.build.date }}

    steps:
      - id: checkout
        uses: actions/checkout@v4

      - id: build
        uses: ./.github/actions/build